// These tests covered the old /api/execute-action endpoint in the custom HTTP server.
// The server has been replaced by the platform-kit createConnectorServer (POST /execute).
// Action handler tests live in packages/discord-bot-connector/src/__tests__/.
